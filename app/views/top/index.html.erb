<h1  class="text-4xl font-bold">Top#index</h1>
<p class="italic">Find me in app/views/top/index.html.erb</p>

<% if current_user %>
  <div class="card text-center pt-5 mx-auto" style="width:23rem">
    <h1>ログインできた！</h1>
    <p>full_name: <%= current_user.full_name %></p>

    <p>username: <%= current_user.username %></p>

    <p>e-mail: <%= current_user.email %></p>

    <%= button_to "サインアウト", destroy_user_session_path, class: "fs-6 my-4 text-decoration-none text-info", method: :delete %>
  </div>

<% elsif %>
  <%= render ReactComponent.new(component: "Sample", raw_props: {message: "hoge"}) %>
<% end %>

<%= render ReactComponent.new(component: "App", raw_props: {message: "hoge"}) %>
